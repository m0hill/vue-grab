{
  "name": "react-grab-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "React-grab and Vue-grab monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter react-grab build && pnpm --filter vue-grab build",
    "build:react": "pnpm --filter react-grab build",
    "build:vue": "pnpm --filter vue-grab build",
    "dev": "pnpm --filter react-grab dev",
    "dev:react": "pnpm --filter react-grab dev",
    "dev:vue": "pnpm --filter vue-grab dev",
    "lint": "pnpm --filter react-grab lint && pnpm --filter vue-grab lint",
    "lint:fix": "pnpm --filter react-grab lint:fix && pnpm --filter vue-grab lint:fix",
    "format": "prettier --write .",
    "check": "pnpm --filter react-grab check && pnpm --filter vue-grab check",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
